一种可并堆,有堆的特性,可合并
外节点 为子节点数小于两个的节点
一个节点的 dist 为 其 到子树中最近的外节点所经过的边的数量。空节点的 dist为 0
//左儿子dist>右儿子
//dist[i]=右儿子dist+1
//左偏树深度没有保证
//根的dist<=log(n+1)-1
merge()合并俩树
取极值根(大根堆取max)
根的左儿子为合并后堆的左儿子
递归合并右儿子和另一个堆
合并后左儿子dist1<右儿子dist 交换

